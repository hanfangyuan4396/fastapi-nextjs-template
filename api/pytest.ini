[pytest]
testpaths = tests

# 如需默认开启覆盖率，可取消注释并按需调整覆盖模块名（示例: core 或 wecom 等）
# addopts = -q --cov=service --cov-report=term-missing

# 可选：注册自定义标记，避免 PytestUnknownMarkWarning
markers =
    unit: 标记单元测试（快速、无外部依赖）
    integration: 标记集成测试（依赖外部系统/资源或 HTTP 层）
